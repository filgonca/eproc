## Instruções para Rodar a Automação

### Rodar a Automação pelo Terminal
Para executar a automação diretamente pelo terminal, utilize o seguinte comando:
```bash
ruby run.rb
```

### Apresentar o Layout do Front
Para abrir a interface gráfica (GUI) do projeto, utilize o comando abaixo:
```bash
ocra gui.rb --debug-extract
```